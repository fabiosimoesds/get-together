<div class="container">
  <div class="row">
    <ul>
      <% @albums.each do |a| %>
        <% if a.trip.user == current_user %>
        <li>
          <%= link_to a.trip.address, album_path(a) %> <%= a.id %>
        </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
